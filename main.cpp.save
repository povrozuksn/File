#include <iostream>
#include <fstream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");

    string str;
    string text_question;
    ifstream file("Вопросы.txt");

    while(file.good())
    {
        getline(file, str);
        int pos1 = 
        
        
        
        cout << str << endl;
    }

    return 0;
}
